#include<stdio.h>
int sqrt(int a, int b);
int main()
{
	while(1)
	{
		printf("Calculating x^n using functions\n");
		printf("-------------------------------------------------\n");
		int x,y;
                printf("\nEnter a base value: ");
		scanf("%d",&x);
		printf("\nEnter the power value:");
		scanf("%d",&y);
		int sq= sqrt(x,y);
		printf("\n The X^n value is = %d\n",sq);
		printf("-----------------------------------------------------\n");	}
	
}
int sqrt(int a, int b)
{
	int temp = a;
       for(int i=1;i<b;i++)
       {
	       a *=temp;
       }	  
  return (a);     
}
